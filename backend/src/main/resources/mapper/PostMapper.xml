<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studygram.mapper.UserMapper">
    <insert id="save" parameterType="Post">
        insert into post (user_id, image_url_id, content, likes_id, tags_id, comments_id, created_date, updated_date)
            values(#{userId}, #{imageUrlId}, #{content}, #{likeId}, #{tagId}, #{commentId}, #{createdDate}, #{updatedDate})
    </insert>
</mapper>